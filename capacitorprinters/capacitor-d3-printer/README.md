# @zpos/capacitor-d3-printer

Capacitor plugin scaffold for the D3 printer, to be migrated from the `D3Printer` Cordova project.

This package defines a minimal TypeScript API and Capacitor registration. The native Android implementation should be ported from the existing Cordova sources.
